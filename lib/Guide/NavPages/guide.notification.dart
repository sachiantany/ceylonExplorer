import 'package:flutter/cupertino.dart';

class GuideNotifi extends StatelessWidget {
  const GuideNotifi({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(
        child: Text("Guide Notification Page"),
      ),
    );
  }
}
